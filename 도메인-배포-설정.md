# logpxai.co.kr 도메인 배포 설정

> 작성일: 2026-05-21
> 대상 프로젝트: `donghwaya` (마이보이스스토리 for 키즈) — `C:\IBKS\workspace\sogang-project`

로컬 PC에서 돌아가는 Next.js 앱을 구매한 도메인 `logpxai.co.kr`에 연결하고,
PC 로그온 시 자동으로 살아나도록 구성한 작업 기록.

---

## 1. 전체 구조

```
브라우저
  └─ https://logpxai.co.kr
       └─ Cloudflare (DNS + HTTPS)
            └─ cloudflared 터널 "myvoicekids"
                 └─ localhost:3001  (Next.js dev 서버)
```

| 항목 | 값 |
|---|---|
| 도메인 | `logpxai.co.kr` (Cloudflare DNS) |
| 터널 이름 | `myvoicekids` (UUID `5e8907a6-93c9-4730-87dd-329a83f578e5`) |
| 로컬 포트 | `3001` (Next.js dev 서버, 고정) |
| 앱 | Next.js 16.2.2 (Turbopack), 패키지명 `donghwaya` |
| DB | MySQL `myvoicekids` @ `localhost:3306` |

---

## 2. 한 작업 요약

### 2-1. 앱 실행
- `npm run dev`로 Next.js dev 서버 기동
- 포트 3000은 기존 프로세스가 점유 중이라 **3001번**으로 구동
- `http://localhost:3001` → HTTP 200 정상 확인

### 2-2. 도메인 연결
원래 임시 터널(랜덤 `trycloudflare.com` 주소)만 떠 있어서 도메인 접속 시 **HTTP 530**(터널 다운) 발생.

조치:
1. 임시 quick 터널 프로세스 종료
2. `C:\Users\lpoi0\.cloudflared\config.yml`의 서비스 포트를 `3000` → **`3001`**로 수정
3. `next.config.ts`에 `allowedDevOrigins: ["logpxai.co.kr"]` 추가
   (Next.js dev 서버가 외부 도메인 요청을 차단하지 않도록)
4. 명명 터널 실행: `cloudflared tunnel run myvoicekids`
5. `https://logpxai.co.kr` → **HTTP 200** 정상 확인

### 2-3. 자동 실행 등록
PC 재부팅 후에도 도메인이 자동으로 살아나도록 구성.

- 작업 스케줄러는 **사내 PC 정책으로 차단**(`Access denied`) → 사용 불가
- 대신 **시작프로그램 폴더** 방식으로 등록 (관리자 권한 불필요)
- 자동 실행 스크립트가 **dev 서버 + cloudflared 터널을 함께 기동**

---

## 3. 생성/수정된 파일

| 파일 | 구분 | 내용 |
|---|---|---|
| `next.config.ts` | 수정 | `allowedDevOrigins: ["logpxai.co.kr"]` 추가 |
| `C:\Users\lpoi0\.cloudflared\config.yml` | 수정 | 서비스 포트 `3000` → `3001` |
| `scripts\autostart-domain.ps1` | 신규 | 자동 실행 스크립트 (dev 서버 + 터널 기동) |
| `시작프로그램\logpxai-autostart.cmd` | 신규 | 로그온 시 위 스크립트를 호출하는 진입점 |
| `autostart.log` | 자동 생성 | 자동 실행 기록 로그 |

> 시작프로그램 폴더 경로:
> `C:\Users\lpoi0\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup`
> (탐색기 주소창에 `shell:startup` 입력하면 바로 열림)

---

## 4. 운영 방법

### 상태 확인
```powershell
# 도메인 응답 확인
Invoke-WebRequest https://logpxai.co.kr -UseBasicParsing | Select-Object StatusCode

# 로컬 dev 서버 확인 (3001 리스닝 여부)
Get-NetTCPConnection -LocalPort 3001 -State Listen

# cloudflared 터널 프로세스 확인
Get-Process cloudflared

# 자동 실행 로그 확인
Get-Content C:\IBKS\workspace\sogang-project\autostart.log -Tail 10
```

### 수동 실행 (자동 실행이 안 됐을 때)
```powershell
# 자동 실행 스크립트를 직접 한 번 돌리면 됨
powershell -ExecutionPolicy Bypass -File "C:\IBKS\workspace\sogang-project\scripts\autostart-domain.ps1"
```

또는 개별 실행:
```powershell
# 1) dev 서버 (반드시 포트 3001로 고정!)
cd C:\IBKS\workspace\sogang-project
npm run dev -- --port 3001

# 2) cloudflared 터널 (다른 터미널에서)
cloudflared tunnel run myvoicekids
```

### 종료
```powershell
# 터널만 종료
Get-Process cloudflared | Stop-Process -Force

# dev 서버 종료 — 3001 포트를 쓰는 node 프로세스 종료
Get-NetTCPConnection -LocalPort 3001 -State Listen |
  ForEach-Object { Stop-Process -Id $_.OwningProcess -Force }
```

### 자동 실행 끄기
시작프로그램 폴더에서 `logpxai-autostart.cmd` 파일을 삭제하면 끝.

---

## 5. 주의사항 / 트러블슈팅

- **"로그온 시" 실행**이다. PC 부팅 후 윈도우 **로그인까지 해야** 도메인이 살아난다.
  부팅 직후(로그인 전) 실행되는 진짜 Windows 서비스는 관리자 권한이 필요한데
  사내 PC에서 막혀 있어 불가능.
- **포트는 반드시 3001**. `config.yml`이 3001을 가리키므로, dev 서버를 다른 포트로
  띄우면 도메인 접속 시 502/530 에러가 난다. 수동 실행 시 `--port 3001` 필수.
- **HTTP 530** = 터널은 살아있는데 도메인↔터널 연결이 끊김. → `cloudflared tunnel run myvoicekids` 재실행.
- **HTTP 502** = 터널은 떴는데 로컬 앱(3001)이 안 떠 있음. → dev 서버 실행 확인.
- dev 서버는 **공개 상태**다. URL을 아는 사람은 누구나 접속 가능하고 디버그 정보도
  노출되므로 데모 용도로만 사용 권장.
- 로그온 직후 콘솔 창이 1~2초 깜빡일 수 있다 (정상).

### 더 안정적으로 운영하려면 (선택)
- 데모/제출용으로는 dev 서버 대신 **프로덕션 빌드**가 더 안정적:
  `npm run build` → `npm start`. 단 코드 변경 시 매번 재빌드 필요.
- 이 경우 `config.yml`의 포트도 프로덕션 서버 포트에 맞춰야 한다.

---

## 6. 참고 명령어

```powershell
# 등록된 cloudflared 터널 목록
cloudflared tunnel list

# 특정 터널 상세 정보
cloudflared tunnel info myvoicekids

# 도메인 DNS 라우팅 (최초 1회, 이미 설정됨)
cloudflared tunnel route dns myvoicekids logpxai.co.kr
```

| 파일 위치 | 설명 |
|---|---|
| `C:\Users\lpoi0\.cloudflared\config.yml` | 터널 라우팅 설정 |
| `C:\Users\lpoi0\.cloudflared\5e8907a6-...json` | 터널 인증 정보 |
| `C:\Users\lpoi0\.cloudflared\cert.pem` | Cloudflare 계정 인증서 |
