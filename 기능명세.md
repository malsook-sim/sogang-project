# 마이보이스스토리 for 키즈 — 기능 명세서

> 서강대학교 AI SW 대학원 · 「생성형 AI의 이해와 활용」 9조 POC

## 1. 프로젝트 개요

부모·보호자가 자신의 목소리를 녹음하면 AI가 그 목소리를 복제하고, 동화를 그 목소리로 읽어주는 모바일·웹 서비스. 아이는 부모의 목소리(또는 기본 제공 목소리)로 동화를 들을 수 있고, 줄거리만 입력하면 우리 아이만의 동화도 AI가 만들어 준다.

- **대상**: 미취학·저학년 아동의 보호자
- **형태**: 반응형 웹 앱 (모바일 우선, 데스크탑 지원)
- **배포 주소**: https://logpxai.co.kr

## 2. 기술 스택

| 구분 | 기술 |
|---|---|
| 프론트·서버 | Next.js 16.2.2 (App Router) · React 19.2 · TypeScript |
| 스타일 | Tailwind CSS v4 |
| 데이터베이스 | MySQL 8 (`mysql2`) |
| 인증 | bcryptjs(비밀번호 해시) · jose(JWT 세션 쿠키) |
| 음성 | ElevenLabs API — Voice Cloning + TTS(`eleven_multilingual_v2`) |
| 동화 생성 | Google Gemini API (`gemini-2.5-flash`, 보조 `gemini-2.5-flash-lite`) |
| 배포 | Cloudflare Tunnel (`cloudflared`) |

## 3. 화면 구성

| 경로 | 화면 | 로그인 |
|---|---|---|
| `/landing` | 서비스 소개 | 불필요 |
| `/` | 홈 — 동화 책장 | 불필요 |
| `/stories/[id]` | 동화 상세 | 불필요 |
| `/player/[id]` | 동화 재생 플레이어 | 불필요 |
| `/voices` | 목소리 선택 | 불필요 |
| `/login` | 로그인 / 회원가입 | — |
| `/create` | AI 동화 만들기 | **필요** |
| `/record` | 내 목소리 녹음 | **필요** |
| `/mypage` | 내 서재 | 불필요(로그인 시 개인화) |

- 모바일(<1024px): 하단 탭바 내비게이션
- 데스크탑(≥1024px): 좌측 사이드바 내비게이션

## 4. 기능 명세

### 4.1 회원 · 인증
- 이메일 + 비밀번호 회원가입 (비밀번호 6자 이상, bcrypt 해시 저장)
- 로그인 / 로그아웃, 가입 즉시 로그인 처리
- 세션: JWT를 httpOnly 쿠키(`mvk_session`, 30일)로 발급
- `proxy` 미들웨어로 로그인 필요 화면(`/create`, `/record`) 보호, 로그인 사용자는 `/login` 진입 시 내 서재로 이동
- 회원가입 시 자녀 이름·나이(선택) 입력

### 4.2 동화 책장 (홈)
- 기본 동화 **34편** 제공 (DB `stories` 테이블)
  - 영어동화 10 · 전래동화 9 · 세계명작 7 · 잠자리동화 4 · 모험 2 · 자연·동물 2
- 카테고리·연령 필터, 제목·교훈 키워드 검색
- 오늘의 추천 · 인기 동화 · 새로 온 동화 · 이어 듣기 큐레이션 섹션
- 동화별 커버 일러스트(SVG), 연령·소요시간·교훈 태그 표시
- 교훈 분류: 용기·우정·정직·나눔·겸손·끈기·지혜·선행·사랑·예절

### 4.3 AI 동화 만들기
- 줄거리·자녀 이름·나이를 입력하면 Gemini가 동화 생성
- 분량 1,000자 이상(목표 1,800~2,600자), 제목·교훈·연령 자동 생성
- 부적절 키워드(선정적·폭력·약물·욕설) 입력 시 생성 차단 및 안내
- 모델 혼잡(503) 시 재시도 + 보조 모델 자동 전환
- 생성한 동화는 "이 동화 저장하기"로 내 서재에 보관

### 4.4 목소리 복제
- 안내 문장 5개를 또박또박 녹음 (총 30초 이상)
- 녹음본을 ElevenLabs로 전송해 음성 복제, 결과를 계정에 저장
- 녹음 길이 부족 시 재녹음 안내
- 기본 제공 목소리 4종(따뜻한 이야기꾼·다정한 엄마·밝은 언니·듬직한 아빠) 별도 제공

### 4.5 동화 듣기 (플레이어)
- ElevenLabs TTS로 동화 본문을 음성 합성해 재생
- 기본 목소리 또는 내가 복제한 목소리 선택
- 재생/일시정지, 0.75~1.5배속, 15초 앞·뒤 이동, 진행바 탐색
- 현재 읽는 문단 하이라이트
- 잠자리 타이머(15/30/60분) — 설정 시간 후 자동 정지, 밤(수면) 테마 전환
- 재생 진행률 저장(`play_history`) → 홈 "이어 듣기"로 이어서 청취

### 4.6 내 서재 (마이페이지)
- 등록한 내 목소리 목록 (삭제 가능)
- 저장한 동화(북마크) — 동화 상세에서 하트로 저장, 로그인 필요
- 내가 만든 동화 — AI로 생성·저장한 동화 목록
- 듣기 캘린더 — 동화를 들은 날짜를 월별 달력으로 기록

## 5. 데이터베이스 구조

| 테이블 | 설명 | 주요 컬럼 |
|---|---|---|
| `users` | 회원 | id, email, password_hash, child_name, child_age, created_at |
| `voices` | 복제한 목소리 | id, user_id, elevenlabs_voice_id, name, emoji, created_at |
| `bookmarks` | 저장한 동화 | user_id, story_id, created_at |
| `user_stories` | AI로 만든 동화 | id, user_id, title, content, morals, age_min, age_max, duration_min, created_at |
| `stories` | 기본 동화 카탈로그 | id, title, content, age_min, age_max, morals, is_premium, category, duration_min, sort_order |
| `play_history` | 재생 진행률·듣기 기록 | user_id, story_id, progress_sec, duration_sec, updated_at |
| `audio_cache` | TTS 합성 결과 캐시 | id, user_id, story_id, voice_id, duration_sec, created_at |

- `voices` · `bookmarks` · `user_stories` 는 `users` 에 `ON DELETE CASCADE` 로 연결

## 6. API 명세

| 메서드·경로 | 설명 |
|---|---|
| `POST /api/auth/signup` | 회원가입(+세션 발급) |
| `POST /api/auth/login` | 로그인 |
| `POST /api/auth/logout` | 로그아웃 |
| `GET /api/auth/me` | 현재 로그인 사용자 조회 |
| `GET /api/stories` | 기본 동화 카탈로그 목록 |
| `GET /api/stories/[id]` | 동화 단건 조회(카탈로그·생성 동화) |
| `GET /api/my-stories` | 내가 만든 동화 목록 |
| `POST /api/my-stories` | 동화 저장 |
| `DELETE /api/my-stories/[id]` | 만든 동화 삭제 |
| `GET /api/bookmarks` | 저장한 동화 목록 |
| `POST /api/bookmarks` | 동화 북마크 토글 |
| `GET /api/voices` | 내 목소리 목록 |
| `DELETE /api/voices/[id]` | 목소리 삭제 |
| `POST /api/voices/clone` | 녹음본으로 목소리 복제 |
| `POST /api/generate-story` | AI 동화 생성 |
| `POST /api/tts` | 동화 본문 음성 합성 |
| `GET /api/play-progress` | 재생 진행률·듣기 기록 조회 |
| `POST /api/play-progress` | 재생 진행률 저장 |

## 7. 배포

- 로컬에서 프로덕션 빌드(`npm run build` → `next start -p 3001`, **3001 포트**) 실행
- Cloudflare Tunnel(`cloudflared`, 터널 `myvoicekids`)이 `logpxai.co.kr` 도메인을 로컬 3001 서버에 연결
- HTTPS 인증서는 Cloudflare가 자동 제공
- DB 초기화: `npm run db:setup`(테이블 생성) → `npm run db:seed`(동화 24편 입력)
