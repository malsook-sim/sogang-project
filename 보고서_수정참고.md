# 최종보고서 수정 참고

> `마이보이스스토리_for_키즈_최종보고서.docx`를 실제 구현에 맞게 보정하기 위한 체크리스트.
> 코드 기준일: 2026-05-28, 커밋 `8fc8b44`.

---

## 1. 기술 스택 — 통째로 교체 필요

보고서 2.1절 "End-to-End 시스템 구성" 표를 아래로 갈음.

| 구분 | 보고서(오류) | 실제 구현 |
|---|---|---|
| 프론트·서버 | Bolt.new (Vercel) | Next.js 16.2.2 (App Router) · React 19.2 · TypeScript |
| 스타일 | — | Tailwind CSS v4 |
| 데이터베이스 | Supabase | MySQL 8 (`mysql2/promise` 풀) |
| 인증 | — | bcryptjs(비밀번호 해시) + jose(JWT, httpOnly 쿠키 `mvk_session`, 30일) |
| 자동화 서버 | Make (Integromat) Webhook | **사용 안 함** — Next.js API Route가 ElevenLabs·Gemini를 직접 호출 |
| 동화 생성 LLM | Claude / Cursor AI | **Google Gemini API** (`gemini-2.5-flash`, 보조 `gemini-2.5-flash-lite`) |
| TTS | ElevenLabs `eleven_multilingual_v2` | ElevenLabs — **복제목소리: `eleven_multilingual_v2`**, **기본목소리: `eleven_turbo_v2_5`** |
| Voice Cloning | ElevenLabs Instant Voice Cloning | 동일 (맞음) |
| 오디오 스토리지 | Cloudflare R2 | **R2 안 씀** — 서버 메모리 LRU 캐시(40개), 원본은 ElevenLabs에 잔존 |
| 배포 | Vercel | Cloudflare Tunnel(`cloudflared`) → 자체 도메인 `logpxai.co.kr` |

---

## 2. 동화 생성 파이프라인 — 사실관계 정정

보고서 2.2절 "4단계 프롬프트 엔지니어링" 표는 실제와 다름. 아래로 보정.

| 단계 | 보고서 주장 | 실제 구현 (`src/app/api/generate-story/route.ts`) |
|---|---|---|
| 1. 메타데이터 추출 | Zero-shot Knowledge Elicitation | 메타데이터 추출 단계 없음 — 사용자가 줄거리·자녀이름·나이·언어를 폼으로 직접 입력 |
| 2. 연령별 본문 생성 | 3~5/6~8/9~11 연령대별 별도 프롬프트 (10/15/20문장) | 단일 프롬프트에 `${childAge}` 동적 삽입. **한글 1,800~2,600자(최소 1,000자)**, **영어 1,200~2,200자** |
| 3. UI 메타 생성 | Few-shot + CoT | Few-shot 예시 없음. **JSON 강제 응답**(`title`, `content`, `contentKo`, `morals[]`, `ageMin`, `ageMax`) |
| 4. LLM-as-Judge 평가 | 별도 평가 모델로 검수 | **미구현**. 대체 수단으로 **BANNED_KEYWORDS 50+개 사전 필터**(선정·폭력·약물·욕설) |

추가로 보고서에 빠진 실제 핵심 동작:
- **재시도/폴백**: 모델당 2회 재시도(900ms 백오프) → 보조 모델로 폴백 → 그래도 실패 시 정적 fallback 동화 반환
- **이중언어**: 영어 동화 생성 시 `contentKo` 필드로 한글 번역도 동시 생성(같은 문단 구조 1:1)

---

## 3. DB 스키마 — Supabase가 아닌 MySQL 6개 테이블

| 테이블 | 설명 |
|---|---|
| `users` | 회원 (email, password_hash, child_name, child_age) |
| `voices` | 복제 목소리 메타 (elevenlabs_voice_id, name, emoji, description) |
| `stories` | **기본 동화 24편** (전래 9 · 세계명작 7 · 잠자리 4 · 모험 2 · 자연동물 2) |
| `user_stories` | 사용자가 AI로 생성·저장한 동화 (content + content_ko) |
| `bookmarks` | 북마크 |
| `play_history` | 재생 진행도 (이어듣기) |

> 보고서의 "Supabase stories 테이블에 동화 4편"은 사실과 다름. **24편 카탈로그 + 사용자별 생성분**.

---

## 4. TTS 설정 — 보고서 트러블슈팅 보정

`src/app/api/tts/route.ts` 실제 voice_settings:

```js
// 복제 목소리 (eleven_multilingual_v2)
{ stability: 0.5, similarity_boost: 0.9, style: 0, use_speaker_boost: true }

// 기본 목소리 (eleven_turbo_v2_5)
{ stability: 0.5, similarity_boost: 0.75 }
```

- 보고서의 SSML `<break time="1.0s"/>` 활용 → **코드엔 SSML 없음**. 삭제 또는 "향후 과제"로 이동
- 텍스트는 5000자에서 절단(ElevenLabs 단일 요청 한도)
- 같은 (voiceId, text) 조합은 메모리 캐시 HIT → 즉시 응답

---

## 5. 보고서가 빠뜨린 핵심 기능 (추가 권장)

| 기능 | 위치 | 메모 |
|---|---|---|
| 회원/인증 시스템 | `src/lib/auth.ts`, `src/proxy.ts` | JWT 쿠키 + 라우트 보호 미들웨어, `/create`·`/record` 로그인 필요 |
| 북마크 + 이어듣기 | `bookmarks`, `play_history` 테이블 | 동화 상세 하트 저장 / 진행도 자동 저장 |
| 잠자리 타이머 | 플레이어 페이지 | 15·30·60분 자동 정지 — 키즈 서비스 UX 차별점 |
| 플레이어 컨트롤 | `src/app/player` | 0.75~1.5배속, 15초 점프, 진행바, 문단 하이라이트 |
| BGM | `scripts/gen-bgm.ts`, `public/bgm/` | 동화 배경음악 합성 |
| 부적절 키워드 차단 | `BANNED_KEYWORDS` (50+개) | 선정·폭력·약물·욕설 사전 차단 |
| 반응형 내비 | 레이아웃 | <1024px 하단 탭바 / ≥1024px 좌측 사이드바 |
| 기본 동화 카탈로그 | `stories` 테이블, 24편 | 카테고리·연령·교훈 태그·재생수 |

---

## 6. 평가 섹션 보강 가이드

보고서 4장 "5명 부모 대상 평가"가 모호함. 다음을 추가하면 설득력↑.

- **응답시간 지표 분리**: 캐시 HIT(<100ms) vs MISS(ElevenLabs 호출 2~5초). 보고서의 "0.3초 이내"는 HIT 한정.
- **목소리 유사도 측정 방법**: 부모 청취 리커트 척도(1~5)인지, 객관 지표(MOS 등)인지 명시
- **LLM-as-Judge는 실제 미구현** → 평가 섹션에선 "키워드 사전필터로 대체"로 정정하고 향후 과제로 이동
- **Gemini 재시도/폴백 성공률** 같은 운영 지표 추가 가능

---

## 7. 향후 과제 — 추가 후보

- 음원 영구 저장소(R2/S3) 도입 — 현재 메모리 캐시는 서버 재시작 시 소실
- LLM-as-Judge 실제 구현 (안전성 자동 검수)
- 동화 일러스트 자동 생성 (이미지 모델 연동)
- 자녀 프로필 다중 지원 (지금은 1명)
- Self-hosting XTTS-v2 — 보고서 기존 항목 유지

---

## 8. 한 줄 요약

> **보고서는 초기 기획(Bolt+Supabase+Make+R2+Claude) 그대로고, 실제 구현은 Next.js+MySQL+Gemini+Cloudflare Tunnel 스택으로 전환됨.** 2장(기술 스택)과 2.2절(프롬프트 엔지니어링)은 통째 재작성, 나머지 장은 위 항목대로 정정·보강.
